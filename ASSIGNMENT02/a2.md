Part A
1. Insert

Before: data1 <-> data2 <-> data3 <-> data4

After inserting the new node in the correct place:

data1 <-> data2 <-> data <-> data3 <-> data4

Changes:

data2.next = data

data.prev = data2

data.next = data3

data3.prev = data

2. Delete

Before: data1 <-> data2 <-> data3 <-> data4

After deleting data2: data1 <-> data3 <-> data4

Changes: data1.next = data3

data3.prev = data1


3. is_present

Before and after stay the same:

data1 <-> data2 <-> data3 <-> data4

This function only chec ks if the value is in the list.
It does not change t he list.


Part C: Analysis

insertself, data O(n)
This function keeps the list sorted. To do that, it may need to go through the list to find the correct place for the ne w value. In the worst case, it may check almost every node before inserting. Because of that, the time complexity is O(n).

remove(self, data)  O(n)
this function looks for a node that contains the given data. In the worst case, the value may be near the end of the list or may not be inn the llist at all, so the function ma need to check all nodes. Therefore, the time complexity is O(n).

is_present(self, data) O(n)
This function searches the list to see whether the given value exists. In the worst case, it may need to go through all nodes before fiinding the value or deciding it is not there. So its time complexity iis O(n).

(self) O(1)
This function returns the number of values in the list. If the list keeps a count variable and updates it whenever a node is inserrted or removed, then getting the length does not require traversing the list. It simply returns the stored count. So the time complexity is O(1).

Final Answer
insert(self, data) = O(n)
remove(self, data) = O(n)
is_present(self, data) = O(n)
__len__(self) = O(1)